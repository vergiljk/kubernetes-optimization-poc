{
  "name": "k8s-optimizer-sdk",
  "version": "1.0.0",
  "description": "Kubernetes Resource Optimizer using Claude Agent SDK",
  "main": "optimizer.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/optimizer.js",
    "dev": "ts-node optimizer.ts",
    "optimize": "npm run build && npm run start"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.19",
    "@anthropic-ai/claude-code": "^2.0.19",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}